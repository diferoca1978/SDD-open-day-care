{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "playwright": {
      "type": "local",
      "command": ["pnpm", "exec", "playwright-mcp", "--browser", "chromium"],
      "enabled": true
    }
  }
}
